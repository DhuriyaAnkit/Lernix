import React from "react"
import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'

import './globals.css'

const _geist = Geist({ subsets: ['latin'] })
const _geistMono = Geist_Mono({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Lernix | Learn Anything - Online Courses for Everyone',
  description: 'Master AI, machine learning, deep learning, and more from industry experts. Comprehensive courses with lifetime access and certificates.',
  generator: 'v0.app',
  keywords: ['AI', 'Machine Learning', 'Deep Learning', 'NLP', 'Computer Vision', 'Online Courses'],
  viewport: {
    width: 'device-width',
    initialScale: 1,
    maximumScale: 1,
    userScalable: false,
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className="font-sans antialiased">{children}</body>
    </html>
  )
}
